
# Required
SESSION_ID=MrNobody~YOUR_SESSION_ID

# Optional: override the default Railway Pair API

# Media provider endpoints.
# The bot sends POST JSON and expects:
# { "url": "https://...", "title": "...", "mimeType": "...", "fileName": "..." }
# or { "base64": "...", "title": "...", "mimeType": "...", "fileName": "..." }
#
# Configure these later when the lawful/authorized API providers are selected.

# Optional bearer keys for providers

# Optional provider timeout

# Phase 2: optional deployment identifier.
# Give every separate bot deployment a unique value.
